const state = {
  logoSrc: ''
}

const mutations = {
}

const actions = {
  // update({ commit }, log) {
  //   commit('ADD_ERROR_LOG', log)
  // },
  // clearErrorLog({ commit }) {
  //   commit('CLEAR_ERROR_LOG')
  // }
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
